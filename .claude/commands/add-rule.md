# /add-rule

$ARGUMENTS

Append the provided rule to `CLAUDE.md` under the `## Rules` section. Then commit the change with the message `docs(claude): add rule`.

Example usage:
```
/add-rule Always extract magic numbers into named constants
```
