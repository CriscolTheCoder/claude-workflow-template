# Project Context

## Tech Stack
- Language: (update per project)
- Package Manager: (update per project)
- Framework: (update per project)

## Rules
- Always run the linter/formatting before committing
- Never commit secrets, `.env` files, or local config
- Prefer explicit types over `any`
- When adding a dependency, update this file
- Never use `enum`; always prefer literal union types
- Always use `bun` instead of `npm` where available

## Workflow
- Start every session in Plan mode (Shift+Tab twice)
- Use `/commit-push` when ready to create a PR
- Use `/add-rule` to append new learnings from mistakes
- Use `/review-pr` to analyze an open PR

## Auto-generated Rules from PR Feedback
<!-- GitHub Action appends rules here automatically when @claude is tagged in PR comments -->
